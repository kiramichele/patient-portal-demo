[build]
  base = "app"
  publish = ".netlify"
  packageManager = "npm"
  command = "npm install && npm run build"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
